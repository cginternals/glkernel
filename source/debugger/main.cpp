
#include <glkernel/white_noise.h>


int main(int /*argc*/, char * /*argv*/[])
{
    //auto noise = glkernel::Kernel<glm::vec4>(16, 20);

    //glkernel::white_noise(noise, glm::vec0.f, 1.f);
    //glkernel::white_noise(noise, 0.f, 1.f);

    //noise2[6] = glm::vec2(1.f, 2.f);

    //noise2.element(9, 2) = glm::vec2(3.f, 4.f);

    // ToDo: glkernel::white_noise(noise2);

    return 0;
}
